[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fastapi-microservice"
version = "0.1.0"
description = "A simple FastAPI microservice with tests and Dockerfile."
readme = "README.md"
requires-python = ">=3.10"
